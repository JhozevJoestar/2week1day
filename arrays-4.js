let arr = ['Python', 'JS', 'php', 'Basic', 'Paskal', 'C#', 'C++', 'Ruby', 'C', 'Objective-C'];
let i = 0;
while (i<arr.length) {
      if (arr[i].length>3){
    console.log(arr[i])
    }
    i++
}