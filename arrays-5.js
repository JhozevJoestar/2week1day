let arr = ['Сахруддин', 'Турпал', 'Ислам', 'Адам', 'Альви', 'Якуб', 'Юсуп', 'Халид', 'Салман', 'Адамас'];
for (let i=0; i<arr.length; i++){
    if(arr[i][0] === 'А'){
        console.log(arr[i])
    }
}